filebeat.inputs:
- type: filestream
  id: my-filestream-id
  enabled: true
  paths:
    - /var/log/nginx/access.log
  
  fields: filebeat_nginx

output.logstash:
  enabled: true
  hosts: ["logstash:5044"]